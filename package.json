{
  "name": "recipe-community-diploma",
  "private": true,
  "description": "Run all package tests from repo root",
  "scripts": {
    "test": "npm run test:backend && npm run test:frontend",
    "test:backend": "npm --prefix backend run test",
    "test:frontend": "npm --prefix frontend run test",
    "test:ci": "npm --prefix backend run test:ci && npm --prefix frontend run test"
  }
}
